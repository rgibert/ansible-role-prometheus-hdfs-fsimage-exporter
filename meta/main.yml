---
galaxy_info:
  role_name: prometheus_hdfs_fsimage_exporter
  author: Richard Gibert
  company: Gibert.ca
  description: Installs a HDFS FSImage exporter for Prometheus
  license: GPLv3
  min_ansible_version: 2.2
  platforms:
    - name: EL
      versions:
        - 6
        - 7
    - name: Ubuntu
      versions:
        - bionic
        - eoan
        - trusty
        - xenial
  galaxy_tags:
    - prometheus
    - hdfs
    - hdfsfsimage
    - prometheusnodeexporter
dependencies:
  - role: rgibert.user_setup
    user_setup_group: "{{ single_binary_service_user }}"
    user_setup_username: "{{ single_binary_service_group }}"
    user_setup_shell: /sbin/nologin
    tags: prometheus_hdfs_fsimage_exporter
    become: true
    become_user: root
